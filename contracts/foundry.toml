[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
monad_testnet = "${PODARENA_RPC_URL}"

[etherscan]
monad_testnet = { key = "unused", url = "https://testnet.monadvision.com/api" }
